# 차량 관리 앱 개발 가이드 (Development Guide)

이 문서는 현재 개발된 **차량 관리 앱**의 실행 방법, 데이터 관리, 그리고 추후 개발 재개 방법을 설명합니다.

## 1. 프로젝트 실행 방법

이 프로젝트는 웹 애플리케이션입니다. 개발 서버를 실행하여 브라우저에서 확인할 수 있습니다.

1. **터미널(PowerShell/CMD)**을 엽니다.
2. 프로젝트 폴더로 이동합니다: `cd "C:\Users\black\OneDrive\바탕 화면\차량관리앱개발"`
3. 다음 명령어를 입력합니다:

    ```bash
    npm run dev
    ```

4. 브라우저에서 `http://localhost:5173` 주소로 접속합니다.

## 2. 데이터 관리 (중요)

이 앱은 **Google Firebase (Firestore)**를 데이터베이스로 사용합니다.
따라서, **앱을 끄거나 컴퓨터를 재부팅해도 데이터는 안전하게 클라우드에 저장**되어 있습니다.

### 데이터 초기화 및 대량 등록 (CSV)

`관리자 페이지 > 설정` 탭에서 데이터를 초기화하거나 엑셀 데이터를 일괄 등록할 수 있습니다.

**[CSV 데이터 형식]**
제공해주신 25개+ 항목을 포함하여 대부분의 자동차 등록원부 데이터를 지원합니다.
엑셀에서 복사하여 붙여넣을 때 다음 순서를 참고하거나, 스마트한 자동 매핑 기능을 이용하세요.

* 주요 필드: 차명, 자동차등록번호, 소유자 명칭, 모델연도, 연료의종류, 보험사 등

## 3. 개발 재개 방법 (AI와 협업하기)

나중에 이 프로젝트를 다시 수정하거나 기능을 추가하고 싶다면, 저(AI)에게 **이 폴더를 그대로 연결**해주시면 됩니다.

1. **폴더 열기**: 작업하던 `차량관리앱개발` 폴더를 엽니다.
2. **질문하기**: "지난번에 만들던 차량 관리 앱 수정하고 싶어"라고 말씀해주세요.
3. **코드 인식**: 저는 이 폴더 안의 소스 코드(`src/`)와 설정 파일들을 읽어서, **현재 상태를 정확히 파악**하고 이어서 작업을 진행할 수 있습니다.

## 4. 백업 권고

* **소스 코드**: 현재 폴더(`차량관리앱개발`)가 가장 중요합니다. OneDrive에 저장되어 있으니 자동 동기화되지만, 불안하다면 폴더 전체를 압축해서 따로 보관하세요.
* **데이터**: `Firebase`에 자동 저장되지만, **등록에 사용한 엑셀(CSV) 원본 파일**은 반드시 따로 가지고 계시는 것이 좋습니다. (나중에 초기화 후 다시 올릴 때 필요)

## 5. 버전 이력 (Version History)

### v5.2.1 (Current Stable)

* **운행기록 시트 수신 오류 해결**: 관내출장 여부와 상관없이 모든 운행기록이 '운행일지 시트(Script 1)'에 즉시 저장되도록 전송 로직 통합.
* **관내출장 프로세스 개선**: 관내출장 신청 시 '승인대기' 상태 DB 기록 및 Slack 알림 기능 분리 (시트 전송과 의존성 제거).
* **데이터 정합성 강화**: 주행거리 입력 시 총 주행거리/누적 거리 자동 계산 로직 개선.
* **정비 항목 설정**: 관리자 설정에서 정비 항목(엔진오일 등)을 커스텀하고 주기에 따라 Slack 알림 발송 기능 추가.
* **매뉴얼 업데이트**: 사용자 가이드(UserGuideManual) 최신화 (간편 주행 삭제, 다중 목적지 안내 추가).
