# Firebase 설정 및 무료 요금제 가이드

파이어베이스(Firebase)를 처음 사용하시는 분들을 위한 **프로젝트 생성부터 API 키 발급, 요금 안전 장치 확인**까지의 단계별 가이드입니다.

---

## 1. Firebase 프로젝트 생성 (무료 요금제 확인)

가장 중요한 **'요금이 나오지 않게 하는 법'**은 **Spark 요금제(무료)**를 유지하는 것입니다. 별도로 카드를 등록하지 않으면 요금이 청구되지 않습니다.

1.  [Firebase 콘솔(console.firebase.google.com)](https://console.firebase.google.com/)에 접속하여 구글 계정으로 로그인합니다.
2.  **"프로젝트 만들기" (Create a project)** 버튼을 클릭합니다.
3.  **프로젝트 이름 입력**: 예: `donghaeng-village` 입력 후 계속.
4.  **Google 애널리틱스**: "지금은 사용 안 함"으로 하셔도 되고, 켜셔도 무료입니다. (편의상 끄고 진행하셔도 됩니다).
5.  **프로젝트 만들기** 클릭 후 완료될 때까지 기다립니다.

> **💡 요금 안전 체크**: 
> 프로젝트가 생성되면 왼쪽 메뉴 최하단에 **"Spark 요금제(무료)"**라고 표시되어 있는지 확인하세요. 이 상태에서는 사용량이 초과되면 서비스가 잠시 멈출 뿐, **돈이 청구되지 않습니다.** (안심하세요!)

---

## 2. 웹 앱 등록 및 API 키 발급

이제 우리가 만든 React 앱과 연결할 열쇠(Key)를 받아야 합니다.

1.  프로젝트 개요(메인 화면) 중앙에 있는 아이콘들 중 **`</>` (웹)** 아이콘을 클릭합니다.
2.  **앱 닉네임 등록**: `동행빌리지 웹` 등 원하는 이름을 입력하고 **"앱 등록"** 클릭. (호스팅 설정 체크박스는 지금은 건너뛰세요).
3.  **SDK 설정 및 구성**:
    *   화면에 코드가 뜹니다. `const firebaseConfig = { ... }` 부분이 보일 것입니다.
    *   이 `{ ... }` 안에 있는 내용들이 바로 **API 키**입니다.
    *   이 내용을 복사해둡니다.

    ```javascript
    // 예시 (복사할 부분)
    apiKey: "AIzaSyD...",
    authDomain: "...",
    projectId: "...",
    storageBucket: "...",
    messagingSenderId: "...",
    appId: "..."
    ```

---

## 3. 코드에 적용하기

아까 복사한 내용을 프로젝트의 `src/firebase.ts` 파일에 붙여넣습니다.

1.  내 컴퓨터의 프로젝트 폴더에서 `src/firebase.ts` 파일을 엽니다.
2.  기존에 적려있던 가짜 내용(`apiKey: "YOUR_API_KEY"`)을 지우고, 방금 복사한 값으로 채워 넣습니다.
3.  `Ctrl + S` 를 눌러 저장합니다.

---

## 4. 필수 설정 (로그인 & 데이터베이스)

키만 넣는다고 바로 작동하지 않습니다. **인증**과 **데이터베이스**를 켜줘야 합니다.

### A. 인증(Authentication) - 구글 로그인 켜기
1.  Firebase 콘솔 왼쪽 메뉴에서 **빌드(Build) > Authentication** 클릭.
2.  **"시작하기"** 버튼 클릭.
3.  **"Sign-in method"** 탭에서 **"Google"** 클릭.
4.  **"사용 설정(Enable)"** 스위치를 켭니다.
5.  **프로젝트 지원 이메일**에 본인의 이메일을 선택하고 **저장(Save)**합니다.
    *   *이제 회사 구글 계정(@together63.kr)으로 바로 로그인이 가능합니다.*

### B. 데이터베이스(Firestore) 켜기
1.  왼쪽 메뉴에서 **빌드(Build) > Firestore Database** 클릭.
2.  **"데이터베이스 만들기"** 클릭.
3.  위치(Location)는 `nam5 (us-central)` 또는 가까운 곳 선택 (기본값 추천).
4.  **보안 규칙(Rules)** 설정:
    *   **"프로덕션 모드에서 시작"** 선택 후 다음 -> 사용 설정.

---

## ✅ 요약

1.  **Spark 요금제(기본)** 상태면 카드 등록 없이는 절대 요금이 청구되지 않으니 안심하세요.
2.  웹사이트에서 프로젝트 생성 -> 웹 앱 추가 -> 키 복사.
3.  `src/firebase.ts` 자리에 붙여넣기.
4.  콘솔에서 **Authentication(구글 로그인 ON)**과 **Firestore(DB)** 메뉴 들어가서 "시작하기" 버튼 누르기.

이제 `npm run dev`로 앱을 실행하고 **"Google 계정으로 로그인"** 버튼을 눌러보세요!
